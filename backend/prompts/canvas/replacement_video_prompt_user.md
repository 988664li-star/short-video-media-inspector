请根据下面的结构化镜头信息、随后提供的原片段分镜图和目标素材图，输出一段可直接发给 Seedance 的主体替换指令。

镜头信息：
$shot_context_json

待替换主体与 Seedance 图片绑定：
$subjects_json

图片顺序：先提供原片段分镜图；其后每张目标素材图都会标明它绑定的源主体。注意：分镜图仅用于理解，最终 Seedance 指令只能引用 `@视频1` 及每个主体绑定的 `@图片N`。请只依据图片中可确认的事实写指令。
