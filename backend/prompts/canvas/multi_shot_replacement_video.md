对 @视频1 执行主体替换编辑。完整处理这段连续原视频：不裁剪、不延长、不拼接、不重拍。

替换任务：
$replacement_items

编辑要求：
- 仅编辑上述明确指定的主体；原视频的时长、镜头顺序、运镜、人物动作、背景、构图、光线、遮挡和其余物品保持不变。
- 每个目标图片引用只对应它绑定的源主体，不得串用、混淆或交换外观。
- 同类主体出现多件时逐一一对一替换，数量不得变化；每件目标对象继承对应源对象的位置、大小、朝向、动作、穿戴、手持或支撑关系。
- 多个被替换主体存在穿戴、接触或遮挡关系时，保持原有层级和相互作用，一次完成全部替换，不得让后一次编辑破坏前一主体。
- 保留原视频已有的字幕、屏幕文字、贴纸、价格标签和 UI，不翻译、不改写、不删除、不新增。
- 每个目标对象以各自绑定的参考图为唯一外观依据，保持结构、材质、颜色和细节一致；画面稳定，动作连贯自然。
