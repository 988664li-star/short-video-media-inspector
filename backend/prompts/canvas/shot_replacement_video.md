任务：仅替换 @视频1 中指定的源对象。完整处理当前 @视频1，不要裁剪、延长、拼接、重排镜头或重拍画面。

输入定义：
- @视频1 是当前原始镜头，定义镜头时长、动作、运镜、人物、背景、构图、遮挡、光线和所有未替换内容。
- $target_image_references 是目标对象参考图，只定义目标对象的外观、颜色、材质、结构和细节；不得理解为新的场景、人物或镜头。

替换目标：
- 源对象：$source_object_name（$source_object_description）。
- 目标对象：$target_description。
- 当前镜头中的对象状态：$shot_action。

执行规则：
- 只将当前镜头中的“$source_object_name”替换为目标对象。目标对象必须严格以参考图为准，不得混入源对象的颜色、轮廓、文字、品牌或结构。
- 保持原镜头的动作、运镜、人物、背景、构图、透视、景深、遮挡关系、光线和未提及内容不变。
- 目标对象必须继承源对象原有的位置、大小比例、朝向、握持、支撑和交互状态；不得新增第二件对象或原片不存在的动作、道具、效果。

输出规则：画面稳定、动作连贯、自然色彩。不要新增场景、字幕、文字、水印或 Logo；不要变形、穿模、闪烁、卡顿或改变未替换内容。
