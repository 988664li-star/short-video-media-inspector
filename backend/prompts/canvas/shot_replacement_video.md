对 @视频1 执行主体替换编辑。完整处理这段连续原视频：不裁剪、不延长、不拼接、不重拍。

替换任务：
- 将视频中的“$source_object_name”（$source_object_description）全部替换为 $target_image_references 所示的目标对象。
- 目标对象说明：$target_description。
- 当前片段中的出现状态：$shot_action。

编辑要求：
- 仅编辑该主体；原视频的时长、镜头顺序、运镜、人物动作、背景、构图、光线、遮挡和其余物品保持不变。
- 同类主体出现多件时逐一一对一替换，数量不得变化；每件目标对象继承对应源对象的位置、大小、朝向、手持或支撑关系。
- 保留原视频已有的字幕、屏幕文字、贴纸、价格标签和 UI，不翻译、不改写、不删除、不新增。
- 目标对象以参考图为唯一外观依据，保持结构、材质、颜色和细节一致；画面稳定，动作连贯自然。
