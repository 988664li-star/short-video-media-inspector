图1是原视频连续片段 $segment_id 的干净多镜头拼图，子画面按时间顺序排列。

输入优先级：
- 图1 是子画面顺序、场景、人物、手部、动作、机位、构图、透视、光线、遮挡关系和非替换内容的唯一依据。
- 图2及后续产品参考图只定义目标产品外观，不得用于生成新的背景、道具、人物、姿势或镜头。

目标产品身份卡：
$product_definitions

替换动作：仅在图1中源产品实际出现的子画面里，$replacement_actions。

严格保留规则：
- 逐格编辑图1；不得改变拼图画布比例、子画面数量、时间顺序、间隔或边框，不得把多个子画面合成新场景。
- 只替换源产品覆盖的区域；人物、手部动作、背景、台面、道具、机位、构图、透视、光线及其他未提及内容必须保持原样。
- 替换后的产品在所有子画面中必须是同一件同款实体，且继承源对象原有的位置、比例、朝向、遮挡、握持、支撑、发光或喷雾等状态。不得残留源产品的形状、颜色、结构、文字或品牌；不得新增第二件产品或原片不存在的效果。

输出规则：不要新增字幕、文字、水印或 Logo；不要重绘场景、改变人物、改变背景或改变画面风格。
