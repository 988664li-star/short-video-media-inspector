import {
  CanvasWorkspaceController,
  type CanvasWorkspaceProps,
} from "./CanvasWorkspaceController";

export function CanvasWorkspace(props: CanvasWorkspaceProps) {
  return <CanvasWorkspaceController {...props} />;
}
